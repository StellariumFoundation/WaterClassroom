{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "build": "mkdir -p dist && npx @tailwindcss/cli -i src/index.css -o dist/index.css --minify && esbuild src/main.tsx --bundle --minify --sourcemap --external:*.css --outfile=dist/index.js && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs && cp index.html dist/index.html && cp -r public/* dist/",
    "start": "node dist/server.cjs",
    "clean": "rm -rf dist server.js data",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^2.4.0",
    "@hono/node-server": "^1.13.7",
    "@libsql/client": "^0.17.3",
    "dotenv": "^17.2.3",
    "hono": "^4.6.14",
    "lucide-react": "^0.546.0",
    "motion": "^12.23.24",
    "react": "^19.0.1",
    "react-dom": "^19.0.1",
    "stripe": "^22.2.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.14",
    "@types/node": "^22.14.0",
    "autoprefixer": "^10.4.21",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.1.14",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2"
  }
}
