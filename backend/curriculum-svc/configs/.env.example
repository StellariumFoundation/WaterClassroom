# Environment specific settings (these override config.yaml)
ENV=development

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8082
GRPC_PORT=50053

# Logging Configuration
LOG_LEVEL=info

# PostgreSQL Configuration
POSTGRES_URI="postgres://youruser:yourpassword@localhost:5432/water_classroom_curriculum?sslmode=disable"
POSTGRES_MAX_OPEN_CONNS=10
POSTGRES_MAX_IDLE_CONNS=10
POSTGRES_CONN_MAX_LIFETIME=5m

# Add other service-specific environment variables here if needed
# e.g., API_KEYS, external service URLs, etc.
